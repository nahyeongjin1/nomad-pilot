# === Application ===
NODE_ENV=development
PORT=3000

# === Database ===
# Railway: DATABASE_URL is set automatically
# Local: use individual params below
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=nomad_pilot

# === CORS ===
CORS_ORIGIN=http://localhost:5173

# === Google Places API ===
# Browser key: configured in Google Cloud Console with HTTP Referrer restriction
# Server key: separate key for Places API REST (future use)
# GOOGLE_PLACES_API_KEY=your_browser_key_here