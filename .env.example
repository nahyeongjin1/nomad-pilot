# === Application ===
NODE_ENV=development
PORT=3000

# === Database ===
# Railway: DATABASE_URL is set automatically
# Local: use individual params below
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=nomad_pilot

# === CORS ===
CORS_ORIGIN=http://localhost:5173

# === Amadeus API (Flight Search) ===
# TODO: https://developers.amadeus.com/ 가입 → My Self-Service Workspace → Create New App
#       API Key = CLIENT_ID, API Secret = CLIENT_SECRET
#       Test 환경: 월 2,000 req, BASE_URL=https://test.api.amadeus.com
#       Production: 별도 신청 후 BASE_URL=https://api.amadeus.com
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
AMADEUS_BASE_URL=https://test.api.amadeus.com

# === Travelpayouts (Affiliate Deeplinks) ===
# TODO: https://www.travelpayouts.com/ 가입 → Dashboard에서 Partner ID(숫자) 확인
#       Programs → Aviasales 프로그램 Join (즉시 승인)
#       비워두면 raw Aviasales URL 반환 (개발 중에는 비워둬도 됨)
TRAVELPAYOUTS_MARKER=

# === Google Places API ===
# Browser key: configured in Google Cloud Console with HTTP Referrer restriction
# Server key: separate key for Places API REST (future use)
# GOOGLE_PLACES_API_KEY=your_browser_key_here
