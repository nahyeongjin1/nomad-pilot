[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/backend/Dockerfile"
watchPatterns = ["apps/backend/**", "packages/shared/**", "pnpm-lock.yaml", "package.json", "pnpm-workspace.yaml", ".npmrc"]

[deploy]
preDeployCommand = "cd apps/backend && node ./node_modules/typeorm/cli.js migration:run -d dist/database/data-source.js"
healthcheckPath = "/api/v1"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10